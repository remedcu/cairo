//! > Test TODO yg

//! > test_function_name
test_function_lowering

//! > function
func foo(mut arr: Array::<felt>, mut b: (felt,)) -> Array::<felt> {
    let y = match f(arr, b) {
        MyEnum::A(x) => arr
    };
    y
}

//! > function_name
foo

//! > module_code
enum MyEnum {
  A: felt,
}
extern func f(ref arr: Array::<felt>, ref b: (felt,)) -> MyEnum nopanic;
// func f(ref arr: Array::<felt>) -> MyEnum { MyEnum::A(3)}
// func identity(m: MyEnum) -> MyEnum {m}

//! > semantic_diagnostics

//! > lowering_diagnostics

//! > lowering_format
